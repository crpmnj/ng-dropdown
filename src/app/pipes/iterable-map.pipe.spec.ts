import { IterableMapPipe } from './iterable-map.pipe';

describe('IterableMapPipe', () => {
  it('create an instance', () => {
    const pipe = new IterableMapPipe();
    expect(pipe).toBeTruthy();
  });
});
