import { DropdownItemsFromArrayPipe } from './dropdown-items-from-array.pipe';

describe('DropdownItemsFromArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new DropdownItemsFromArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
